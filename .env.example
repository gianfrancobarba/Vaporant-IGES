# Credenziali del database per l'ambiente containerizzato.
# Copiare questo file in '.env' e impostare un valore reale:
#   cp .env.example .env
# Docker Compose legge automaticamente il file '.env' nella radice del progetto
# e ne sostituisce i valori nel docker-compose.yml (variabili ${...}).
DB_PASSWORD=changeme
